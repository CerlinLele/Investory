# 第3-1课 落地计划 — 执行总览与快速参考

## 📊 四阶段总览

| Phase | 名称 | 目标 | 时间 | 优先级 | 收益 |
|-------|------|------|------|--------|------|
| 1 | 统一能力台账 | TaskSpec 补充治理元数据 | 0.5天 | P0 | 其它 phase 的基础 |
| 2 | Mock 执行器 | 测试脱离 LLM 依赖 | 1天 | P0 | 测试快 100 倍,解决迭代痛点 |
| 3 | Plan Handler 抽象 | 规划策略可替换 | 1天 | P1 | v1/v2/v3 版本灵活切换 |
| 4 | 治理框架完善 | 审计日志 + 访问控制 | 1天 | P2 | 合规、多租户、权限管理基础 |
| **总计** | — | **完整工具管理体系** | **3.5天** | — | **生产级治理** |

---

## 🎯 关键决策总结

### 为什么这个顺序?

1. **Phase 1 必须先做** — 其它三个都依赖 TaskSpec 的元数据
2. **Phase 2 和 Phase 3 可并行** — 互不依赖
3. **Phase 4 最后做** — 可选但推荐,完善治理

### 为什么不做课程模块二(动态注入)?

你的架构是 **LangGraph 确定性管线**,不是 ReAct agent。模型**不自由选工具**,工具选择由图节点写死或 LLM router 已决定。套用模块二只增加复杂度,没有收益。

### 为什么保留 LoopEngine?

这是为 **第 3-2 课(MCP 工具)预埋** 的。现在保留(成本低),当真正需要外部工具时再启用。见 `PHASE4` 文档的"LoopEngine何时启用"。

---

## 📋 快速参考 — 每个 Phase 的核心改动

### Phase 1 核心改动

```python
# task_spec.py — TaskSpec 新增三字段
@dataclass(slots=True)
class TaskSpec:
    name: str
    prompt_name: str
    input_model: type[BaseModel]
    output_model: type[BaseModel]
    side_effect_level: str = "read"  # ← 新增
    tag: str = ""                    # ← 新增
    desc: str = ""                   # ← 新增

# routing.py — 新增查询函数
list_specs_by_tag(tag: str)
list_specs_by_side_effect(level: str)
```

**改动总数**: ~50 行代码

---

### Phase 2 核心改动

```python
# runtime/execution/mock_task_executor.py — 新文件
class MockTaskExecutor(TaskExecutor):
    def __init__(self, fixtures: dict[str, TaskResult]):
        self.fixtures = fixtures
    
    def run(self, spec, payload):
        return self.fixtures[spec.name]

# tests/investment_document_review/test_flow_with_mock.py — 新测试
def test_complete_flow_under_200ms():
    executor = MockTaskExecutor(get_investment_document_review_fixtures())
    flow = build_investment_document_review_flow(executor=executor)
    # Run flow, verify < 200ms
```

**改动总数**: ~300 行代码(包括 fixtures 和测试)

---

### Phase 3 核心改动

```python
# runtime/flow/investment_document_review/plan_handler.py — 新文件
class ReviewPlanHandler(Protocol):
    def build_plan(self, state) -> TodoExecutionPlan: ...

class ChunkRulePlanHandler(ReviewPlanHandler): ...
class LLMPlanHandler(ReviewPlanHandler): ...
class FixedAuditPlanHandler(ReviewPlanHandler): ...

# document_review_flow.py — 改造
class InvestmentDocumentReviewFlow:
    def __init__(self, ..., plan_handler: ReviewPlanHandler | None = None):
        self.plan_handler = plan_handler or self._default_plan_handler()
    
    def generate_review_todo_plan(self, state):
        todo_plan = self.plan_handler.build_plan(state)
        return {"todo_plan": todo_plan}
```

**改动总数**: ~400 行代码(包括 handler 和测试)

---

### Phase 4 核心改动

```python
# runtime/execution/audited_task_executor.py — 新文件
class AuditedTaskExecutor(TaskExecutor):
    def __init__(self, executor, session_id=None):
        self.executor = executor
        self.audit_log = []
    
    def run(self, spec, payload):
        result = self.executor.run(spec, payload)
        self.audit_log.append({...})
        return result

# routing.py — 扩展
validate_task_access(task_name: str, user_role: str) -> bool
```

**改动总数**: ~200 行代码

---

## ✅ 提交检查清单

### Phase 1

```
Commit: feat(tasks): add governance metadata to TaskSpec

[ ] src/investory/agent_core/contracts/task_spec.py 
    - Add side_effect_level, tag, desc fields
    - All with defaults (backward compatible)

[ ] src/investory/agent_core/tasks.py
    - Fill metadata for all 9 TaskSpec instances
    - risk_assessment: side_effect_level="write"
    - Others: side_effect_level="read"
    - Tag allocation: learning/document_review/risk

[ ] src/investory/gateway/routing.py
    - Add list_specs_by_tag()
    - Add list_specs_by_side_effect()
    - Add list_all_specs()
    - Add get_spec_metadata()

[ ] tests/test_tasks_metadata.py (new)
    - test_all_tasks_have_side_effect_level
    - test_list_specs_by_tag works correctly
    - test_list_specs_by_side_effect works correctly
    - test_write_tasks_are_tagged

[ ] pytest tests/test_tasks_metadata.py -v
    => 12/12 passed

[ ] pytest (full suite)
    => no new failures
```

---

### Phase 2

```
Commit: feat(execution): add MockTaskExecutor for fast deterministic testing

[ ] src/investory/agent_core/runtime/execution/__init__.py (new)
    - Export MockTaskExecutor, fixtures

[ ] src/investory/agent_core/runtime/execution/mock_task_executor.py (new)
    - MockTaskExecutor class with call tracking
    - get_call_count(), get_calls(), reset_call_log()

[ ] src/investory/agent_core/runtime/execution/executor_fixtures.py (new)
    - get_investment_document_review_fixtures()
    - get_simple_extraction_fixture()
    - get_failure_fixture()

[ ] tests/investment_document_review/test_flow_with_mock.py (new)
    - test_chunk_review_flow_completes_without_network
    - test_mock_executor_call_tracking
    - test_missing_fixture_raises_error

[ ] tests/investment_document_review/test_flow_mock_performance.py (new)
    - test_complete_flow_under_200ms()

[ ] pytest tests/investment_document_review/test_flow_with_mock.py -v
    => all passed

[ ] pytest tests/investment_document_review/test_flow_mock_performance.py -v
    => all passed, confirms < 200ms

[ ] pytest (full suite)
    => no new failures
```

---

### Phase 3

```
Commit: refactor(plan-handler): extract plan generation into pluggable handlers

[ ] src/investory/agent_core/runtime/flow/investment_document_review/plan_handler.py (new)
    - ReviewPlanHandler (Protocol)
    - ChunkRulePlanHandler
    - LLMPlanHandler
    - FixedAuditPlanHandler
    - DefaultAdaptivePlanHandler

[ ] src/investory/agent_core/runtime/flow/investment_document_review/document_review_flow.py
    - Add plan_handler parameter to __init__
    - Modify generate_review_todo_plan to use plan_handler
    - Remove _build_chunk_review_todo_plan() method
    - Add _default_plan_handler() method

[ ] tests/investment_document_review/test_plan_handlers.py (new)
    - test_chunk_handler_creates_extract_tasks
    - test_chunk_handler_creates_analyze_tasks
    - test_chunk_handler_creates_synthesize_task
    - test_llm_handler_calls_executor
    - test_fixed_handler_simple_plan
    - test_fixed_handler_comprehensive_plan
    - test_default_handler_uses_chunk_for_multi_chunk
    - test_default_handler_uses_llm_for_single_chunk

[ ] pytest tests/investment_document_review/test_plan_handlers.py -v
    => all passed

[ ] pytest (full suite)
    => no new failures
    => existing flow tests still pass (DefaultAdaptivePlanHandler preserves behavior)
```

---

### Phase 4

```
Commit: feat(governance): complete task governance framework

[ ] src/investory/agent_core/runtime/execution/audited_task_executor.py (new)
    - AuditedTaskExecutor class with audit logging
    - get_audit_log(), get_audit_summary()
    - export_audit_log_csv()

[ ] src/investory/gateway/routing.py
    - Add get_specs_for_tags()
    - Add get_write_tasks() / get_read_only_tasks()
    - Add validate_task_access()
    - Expand get_spec_metadata()

[ ] docs/3-1/LoopEngine何时启用.md (new)
    - Why preserved (MCP tool integration prerequisite)
    - When to enable (3 trigger conditions)
    - 3-phase rollout plan
    - Migration checklist

[ ] tests/test_task_governance.py (new)
    - test_list_all_specs_returns_9_tasks
    - test_list_specs_by_tag_*
    - test_list_specs_by_side_effect_*
    - test_admin_can_access_all_tasks
    - test_analyst_can_access_read_tasks
    - test_analyst_cannot_access_write_tasks
    - test_audited_executor_logs_calls
    - test_audited_executor_summary

[ ] pytest tests/test_task_governance.py -v
    => all passed

[ ] pytest (full suite)
    => no new failures
```

---

## 🚀 执行路线建议

### 方案 A:严格顺序(推荐新手)

```
Week 1:
  Mon: Phase 1 (0.5天)
  Tue-Wed: Phase 2 (1天)
  Thu: Phase 3 (1天) — 或和 Phase 2 并行
  Fri: Phase 4 (1天) — 可选

Total: 3.5 天集中工作
```

### 方案 B:并行+优化(推荐有经验的)

```
Day 1:  Phase 1 (0.5天)
Day 2:  Phase 2 并行 Phase 3 (1.5天)
Day 3:  Phase 4 (1天) — 可选

Total: 3 天,因为 Phase 2&3 并行
```

### 方案 C:渐进式(推荐谨慎的)

```
Week 1: Phase 1 + Phase 2 (1.5天)
  — 停下来验证(这已经解决了测试痛点)
  — 收集反馈

Week 2: Phase 3 (1天)
  — 加上版本策略切换
  — 再验证

Week 3: Phase 4 (1天,可选)
  — 如果有合规需求再做
```

---

## 📈 预期收益时间表

### 立即(Phase 1~2,第 1~2 天)
- ✅ 单元测试从"等 30 秒 LLM"变成"100ms 内跑完"
- ✅ CI/CD 流程加快
- ✅ 开发者反馈:测试爽多了

### 短期(Phase 3,第 3 天)
- ✅ 能灵活切换 v1/v2/v3 的规划策略
- ✅ 灰度发布变得容易(不改代码,改参数)
- ✅ A/B 测试不同规划逻辑成为可能

### 中期(Phase 4,第 4 天~周)
- ✅ 审计日志完整,合规不再烦恼
- ✅ 为多租户/角色权限埋下基础
- ✅ 生产故障排查有了清晰的日志链路

### 长期(第 3-2 课)
- ✅ MCP 工具集成时已有成熟的执行框架
- ✅ 新工具类型接入成为标准流程,不是定制开发

---

## 🤔 常见问题

### Q: 能只做 Phase 1 和 2,跳过 3 和 4 吗?
**A**: 可以。Phase 1 和 2 互相独立,都有收益。Phase 3 是为版本策略做准备,Phase 4 是为合规做准备。你可以先做 1&2,看效果后再做 3&4。

### Q: 改动会不会影响现有功能?
**A**: 不会。所有改动都是:
- TaskSpec 新字段有默认值(向后兼容)
- 新类(MockExecutor、PlanHandler)不影响现有流
- 新测试,不修改现有测试
- DefaultAdaptivePlanHandler 保持现有行为

### Q: 能同时进行多个 Phase 吗?
**A**: 可以。Phase 1 是基础,必须先做。Phase 2 和 3 之后可以两人分别并行。Phase 4 可以最后或择时做。

### Q: 需要改 FastAPI 接口吗?
**A**: 不需要。这些改动都在内部架构层。外部 API 保持不变。

### Q: 改完后如何验证成功?
**A**: 
- Phase 1: `pytest tests/test_tasks_metadata.py -v` 全过
- Phase 2: `pytest tests/investment_document_review/test_flow_mock_performance.py` 证明 < 200ms
- Phase 3: 用不同 plan_handler 跑 flow,结果符合预期
- Phase 4: `validate_task_access()` 正确返回权限结果

---

## 📚 文档导航

- **总体方案**: [Investory_第3-1课_工具管理规划系统_业务贴合落地计划.md](./Investory_第3-1课_工具管理规划系统_业务贴合落地计划.md)
- **Phase 1**: [PHASE1_能力台账设计与实施指南.md](./PHASE1_能力台账设计与实施指南.md)
- **Phase 2**: [PHASE2_Mock执行器实现指南.md](./PHASE2_Mock执行器实现指南.md)
- **Phase 3**: [PHASE3_Plan_Handler抽象指南.md](./PHASE3_Plan_Handler抽象指南.md)
- **Phase 4**: [PHASE4_治理框架完善指南.md](./PHASE4_治理框架完善指南.md)
- **LoopEngine 启用**: [LoopEngine何时启用.md](./LoopEngine何时启用.md)(见 Phase 4 文档)

---

## 🎬 下一步

1. **阅读** 总体方案文档,理解为什么这样设计
2. **选择** 执行路线(方案 A/B/C)
3. **开始** Phase 1 — 只需 30 分钟
4. **反馈** 完成后告诉我结果,确认是否进入 Phase 2

**关键**:这不是"一步到位"的大重构。每个 Phase 都是独立的、有明确收益的、可验证的改进。做完 Phase 1 就能看到结果。

---

## 📞 支持

遇到问题时参考:
- 具体代码改动 → 见对应 Phase 的实施指南
- 改动原因 → 见总体方案文档
- 何时启用某个功能 → 见 LoopEngine 启用指南

有任何问题,随时反馈。
